interface:
  display_name: "Eden Grill"
  short_description: "Repo-specific patterns and workflows for eden-grill"
  default_prompt: "Use the eden-grill repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true